<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="Delete_Newline.Views.OCRPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:Delete_Newline"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Grid>
        <ScrollViewer>
            <StackPanel Margin="24"
                        Spacing="16">

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                    <StackPanel Margin="16"
                                Spacing="12">
                        <TextBlock Text="Full Screen OCR Capture"
                                   Style="{ThemeResource SubtitleTextBlockStyle}" />

                        <TextBlock Text="Capture text from any part of your screen using OCR technology."
                                   Style="{ThemeResource BodyTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                        <Button x:Name="StartFullScreenOcrButton"
                                Content="Start Full Screen OCR Capture"
                                Style="{ThemeResource AccentButtonStyle}"
                                HorizontalAlignment="Left"
                                Click="StartFullScreenOcrButton_Click" />
                    </StackPanel>
                </Border>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                    <StackPanel Margin="16"
                                Spacing="12">
                        <TextBlock Text="OCR Language Settings"
                                   Style="{ThemeResource SubtitleTextBlockStyle}" />

                        <TextBlock Text="Choose the language for text recognition."
                                   Style="{ThemeResource BodyTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                        <ComboBox x:Name="LanguageComboBox"
                                  Header="Recognition Language"
                                  HorizontalAlignment="Stretch"
                                  SelectionChanged="LanguageComboBox_SelectionChanged">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DisplayName}" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>
                </Border>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                    <StackPanel Margin="16"
                                Spacing="12">
                        <TextBlock Text="OCR Options"
                                   Style="{ThemeResource SubtitleTextBlockStyle}" />

                        <ToggleSwitch x:Name="SingleLineToggle"
                                      Header="Single Line Mode"
                                      OnContent="Enabled"
                                      OffContent="Disabled" />

                        <TextBlock Text="When enabled, all recognized text will be joined into a single line."
                                   Style="{ThemeResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>